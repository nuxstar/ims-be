/**
 * Contains Data Transfer Objects (DTOs) used for transferring data between
 * layers of the application.
 * The DTOs in this package serve as simple data containers that encapsulate
 * the data to be sent or received by the API endpoints.
 * These DTOs are used to decouple the API layer from the internal domain
 * model and provide a standardized structure for data exchange.
 */
package com.flyerssoft.ams.model.dto;
